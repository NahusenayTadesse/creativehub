node_modules

# Output
.output
.vercel
.netlify
.wrangler
/.svelte-kit
/build

# OS
.DS_Store
Thumbs.db

# Env
.env
.env.*
!.env.example
!.env.test

# Vite
vite.config.js.timestamp-*
vite.config.ts.timestamp-*
# Playwright
test-results
# Paraglide
src/lib/paraglide
project.inlang/cache/

# Seeded sign-in details — not for the repository
ACCOUNTS.md

# Regenerated from the CSV by `npm run import:creators`
scripts/data/creators-import.json

# Uploaded and downloaded files — user content, not source. `FILES_DIR` points
# here by default; the server keeps its own copy.
/.tempFiles

# Playwright MCP scratch output
.playwright-mcp/
build/
build.tar.gz
build.tar
build.zip
build*.tar.gz
build*.zip
